{
  "name": "ai-agent-evals-results-difference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demonstration of how Claude model choice affects agent outcomes — making the case for evals.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "estimate": "tsx src/cli/estimate.ts",
    "sweep": "tsx src/cli/run.ts",
    "report": "tsx src/cli/report.ts",
    "probe-models": "tsx src/cli/probeModels.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1",
    "ajv": "^8.20.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.19.0",
    "typescript": "^5.5.3",
    "vitest": "^2.0.0"
  }
}
